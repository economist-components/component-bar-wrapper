sudo: false
language: node_js
node_js:
  - stable
after_success: >-
  travis-after-all && npm run semantic-release && npm run pages -- --repo
  https://${GH_TOKEN}@github.com/economist-components/component-bar-wrapper.git
